<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sepetim - SendeAl.com</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-orange: #ff6b35;
            --secondary-orange: #ff8c42;
            --light-orange: #fff4f0;
            --dark-orange: #e55a2b;
            --text-dark: #2c3e50;
            --text-light: #6c757d;
            --white: #ffffff;
            --border-light: #e9ecef;
            --shadow: rgba(255, 107, 53, 0.15);
        }

        body {
            background: linear-gradient(135deg, #f8f9fa 0%, var(--light-orange) 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .category-hero {
            background: var(--primary-orange);
            padding: 50px 0 40px;
            position: relative;
            overflow: hidden;
            border-radius: 0 0 50px 50px;
            box-shadow: 0 10px 30px rgba(255, 107, 53, 0.2);
            text-align: center;
            color: var(--white);
        }

            .category-hero::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="rgba(255,255,255,0.15)" stroke-width="0.75"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
                animation: slideGrid 20s linear infinite;
                z-index: 0;
            }

        @@keyframes slideGrid {
            0%

        {
            transform: translateX(0) translateY(0);
        }

        100% {
            transform: translateX(10px) translateY(10px);
        }

        }

        .category-hero-content {
            position: relative;
            z-index: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 50px;
            flex-wrap: wrap;
        }

            .category-hero-content img {
                height: 60px;
                object-fit: contain;
            }

        .category-hero-title {
            font-size: 3rem;
            font-weight: 800;
            margin: 0;
            color: var(--white);
            text-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        .display-4 {
            background: linear-gradient(45deg, var(--primary-orange), var(--secondary-orange));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 800;
            margin-bottom: 2rem;
            position: relative;
        }

            .display-4::after {
                content: '';
                position: absolute;
                bottom: -10px;
                left: 50%;
                transform: translateX(-50%);
                width: 80px;
                height: 4px;
                background: linear-gradient(90deg, var(--primary-orange), var(--secondary-orange));
                border-radius: 2px;
            }

        .table-responsive {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            border: 1px solid var(--border-light);
        }

        .table {
            margin-bottom: 0;
        }

            .table th {
                background: linear-gradient(135deg, var(--primary-orange), var(--secondary-orange));
                color: white;
                font-weight: 600;
                border: none;
                padding: 20px 15px;
                font-size: 0.95rem;
                text-transform: uppercase;
                letter-spacing: 0.5px;
            }

            .table td {
                padding: 20px 15px;
                vertical-align: middle;
                border-bottom: 1px solid var(--border-light);
                transition: all 0.3s ease;
            }

            .table tr:hover td {
                background: var(--light-orange);
            }

            .table tr:last-child td {
                border-bottom: none;
            }

            .table td img {
                border-radius: 8px;
                transition: all 0.3s ease;
                object-fit: cover;
                width: 50px;
                height: 50px;
            }

            .table tr:hover td img {
                transform: scale(1.1);
            }

        .nav-link {
            color: var(--text-dark) !important;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }

            .nav-link:hover {
                color: var(--primary-orange) !important;
                text-decoration: underline;
            }

        .table td:nth-child(3),
        .table td:nth-child(5) {
            color: var(--primary-orange);
            font-weight: 700;
            font-size: 1rem;
        }

        .table td:nth-child(4) {
            color: var(--text-dark);
            font-weight: 600;
        }

        .btn-danger {
            background: linear-gradient(45deg, #dc3545, #c82333);
            border: none;
            border-radius: 8px;
            padding: 8px 12px;
            transition: all 0.3s ease;
            box-shadow: 0 2px 8px rgba(220, 53, 69, 0.3);
        }

            .btn-danger:hover {
                background: linear-gradient(45deg, #c82333, #bd2130);
                transform: translateY(-2px);
                box-shadow: 0 4px 15px rgba(220, 53, 69, 0.4);
            }

        .col-md-4 > div:first-child {
            background: white;
            border-radius: 20px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border: 1px solid var(--border-light);
            margin-bottom: 20px;
        }

        .col-md-4 h4 {
            color: var(--text-dark);
            font-weight: 700;
            margin-bottom: 20px;
            text-align: center;
            position: relative;
        }

            .col-md-4 h4::after {
                content: '';
                position: absolute;
                bottom: -10px;
                left: 50%;
                transform: translateX(-50%);
                width: 60px;
                height: 3px;
                background: linear-gradient(90deg, var(--primary-orange), var(--secondary-orange));
                border-radius: 2px;
            }

        .col-md-4 .table {
            background: transparent;
            box-shadow: none;
            border: none;
        }

            .col-md-4 .table td,
            .col-md-4 .table th {
                border-bottom: 1px solid var(--border-light);
                padding: 15px 0;
            }

            .col-md-4 .table tr:last-child th,
            .col-md-4 .table tr:last-child td {
                border-bottom: 2px solid var(--primary-orange);
                font-weight: 700;
                color: var(--primary-orange);
                font-size: 1.1rem;
            }

        .btn-danger.me-2,
        .btn-success {
            padding: 12px 25px;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
            margin: 5px;
            position: relative;
            overflow: hidden;
        }

        .btn-danger.me-2 {
            background: linear-gradient(45deg, var(--primary-orange), var(--secondary-orange)) !important;
            border: none;
        }

        .btn-success {
            background: linear-gradient(45deg, #28a745, #20c997) !important;
            border: none;
        }

            .btn-danger.me-2::before,
            .btn-success::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
                transition: left 0.5s ease;
            }

            .btn-danger.me-2:hover::before,
            .btn-success:hover::before {
                left: 100%;
            }

        .btn-danger.me-2:hover {
            background: linear-gradient(45deg, var(--dark-orange), var(--primary-orange)) !important;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px var(--shadow);
        }

        .btn-success:hover {
            background: linear-gradient(45deg, #20c997, #17a2b8) !important;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(40, 167, 69, 0.3);
        }

        .alert-warning {
            background: linear-gradient(135deg, #fff3cd, #ffeaa7);
            border: 2px solid var(--secondary-orange);
            border-radius: 15px;
            color: var(--text-dark);
            text-align: center;
            padding: 40px 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

            .alert-warning h3 {
                margin-bottom: 20px;
                color: var(--dark-orange);
                font-weight: 600;
            }

            .alert-warning::before {
                content: '🛒';
                font-size: 3rem;
                display: block;
                margin-bottom: 20px;
            }

        @@media (max-width: 768px) {
            .table th, .table td

        {
            padding: 12px 8px;
            font-size: 0.9rem;
        }

        .table td img {
            width: 40px;
            height: 40px;
        }

        .display-4 {
            font-size: 2rem;
        }

        .col-md-4 > div:first-child {
            padding: 20px;
        }

        .btn-danger.me-2,
        .btn-success {
            padding: 10px 20px;
            font-size: 0.9rem;
            width: 100%;
            margin: 5px 0;
        }

        }

        @@media (max-width: 576px) {
            .table-responsive

        {
            border-radius: 15px;
        }

        .table th,
        .table td {
            padding: 10px 6px;
            font-size: 0.85rem;
        }

        .col-md-4 > div:first-child {
            padding: 15px;
            border-radius: 15px;
        }

        }

        .table tbody tr {
            animation: slideInUp 0.5s ease forwards;
            opacity: 0;
            transform: translateY(20px);
        }

        @@keyframes slideInUp {
            to

        {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .table tbody tr:nth-child(1) {
            animation-delay: 0.1s;
        }

        .table tbody tr:nth-child(2) {
            animation-delay: 0.2s;
        }

        .table tbody tr:nth-child(3) {
            animation-delay: 0.3s;
        }

        .table tbody tr:nth-child(4) {
            animation-delay: 0.4s;
        }

        .table tbody tr:nth-child(5) {
            animation-delay: 0.5s;
        }

        .table tbody tr:nth-child(n+6) {
            animation-delay: 0.6s;
        }

        .shopping-btn {
            display: inline-block;
            padding: 12px 25px;
            background: linear-gradient(45deg, var(--primary-orange), var(--secondary-orange)) !important;
            color: white !important;
            text-decoration: none;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
            margin: 5px;
            position: relative;
            overflow: hidden;
            border: none;
        }

            .shopping-btn::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
                transition: left 0.5s ease;
            }

            .shopping-btn:hover::before {
                left: 100%;
            }

            .shopping-btn:hover {
                background: linear-gradient(45deg, var(--dark-orange), var(--primary-orange)) !important;
                transform: translateY(-2px);
                box-shadow: 0 8px 25px var(--shadow);
                text-decoration: none;
                color: white !important;
            }
    </style>
</head>
<body>
    @model CartViewModel
    @{
        ViewData["Title"] = "Sepetim";
    }

    <section class="container my-5">
        <div class="text-center">
            <h1 class="display-4 mt-2">Sepetim</h1>
        </div>
        <div class="row">
            @if (Model.CartLines.Any())
            {
                <div class="col-md-8">
                    <br />
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Resim</th>
                                    <th>Ürün</th>
                                    <th>Fiyat</th>
                                    <th>Miktar</th>
                                    <th>Toplam</th>
                                    <th>İşlem</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.CartLines)
                                {
                                    <tr>
                                        <td><img src="/Img/Products/@item.Product.Image" alt="@item.Product.Name" /></td>
                                        <td>
                                            <a class="nav-link text-dark" href="/Products/Details/@item.Product.Id">
                                                @item.Product.Name
                                            </a>
                                        </td>
                                        <td>@item.Product.Price.ToString("c")</td>
                                        <td>
                                            @item.Quantity
                                        </td>
                                        <td>@((item.Quantity * item.Product.Price).ToString("c"))</td>
                                        <td>
                                            <form asp-action="Remove" asp-controller="Cart">
                                                <input type="hidden" name="ProductId" value="@item.Product.Id" />
                                                <button class="btn btn-sm btn-danger" type="submit" title="Sepetten Kaldır">
                                                    <i class="bi bi-x-lg"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="col-md-4">
                    <div>
                        <h4>
                            Sepet Toplamı
                        </h4>
                        <table class="table">
                            <tr>
                                <td>Ara Toplam</td>
                                <td>@Model.TotalPrice.ToString("c")</td>
                            </tr>
                            <tr>
                                <td>Kargo</td>
                                <td>
                                    @(Model.TotalPrice > 999 ? "Ücretsiz" : Model.CartLines.Any() ? "₺99" : "₺0")
                                </td>
                            </tr>
                            <tr>
                                <th style="color:white; text-align:center;">Sipariş Toplamı</th>
                                <td style="text-align:center;">
                                    @(Model.TotalPrice > 999 ? Model.TotalPrice.ToString("c") : (Model.CartLines.Any() ? (Model.TotalPrice + 99).ToString("c") : Model.TotalPrice.ToString("c")))
                                </td>
                            </tr>
                        </table>
                        <div class="text-center">
                            <a href="/" class="btn btn-danger me-2">
                                <i class="bi bi-cart me-1"></i>
                                Alışverişe Devam Et
                            </a>
                            <a href="/Cart/Checkout" class="btn btn-success">
                                <i class="bi bi-credit-card me-1"></i>
                                Satın Al
                            </a>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <div class="container">
                    <div class="alert alert-warning my-5">
                        <h3>Sepetinizde Ürün Bulunmamaktadır!</h3>
                        <p class="mb-0">Beğendiğiniz ürünleri sepetinize ekleyerek sipariş verebilirsiniz.</p>
                        <a href="/" class="shopping-btn">
                            <i class="bi bi-shop me-2"></i>Alışverişe Başla
                        </a>
                    </div>
                </div>
            }
        </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll('.btn-danger:not(.me-2)').forEach(button => {
            button.addEventListener('click', function(e) {
                e.preventDefault();

                const productName = this.closest('tr').querySelector('td:nth-child(2) a').textContent.trim();

                if (confirm(`"${productName}" ürününü sepetten kaldırmak istediğinizden emin misiniz?`)) {
                    this.innerHTML = '<i class="bi bi-hourglass-split"></i>';
                    this.disabled = true;

                    const row = this.closest('tr');
                    row.style.transition = 'all 0.5s ease';
                    row.style.opacity = '0.5';
                    row.style.transform = 'translateX(-20px)';

                    setTimeout(() => {
                        this.closest('form').submit();
                    }, 300);
                }
            });
        });

        document.querySelector('.btn-success')?.addEventListener('click', function(e) {
            this.innerHTML = '<i class="bi bi-hourglass-split me-1"></i>Yönlendiriliyor...';
            this.disabled = true;
        });

        window.addEventListener("wheel", function(e) {
            e.preventDefault();
            window.scrollBy({
                top: e.deltaY,
                left: 0,
                behavior: "smooth"
            });
        }, { passive: false });
    </script>
</body>
</html>